#include <bits/stdc++.h>
using namespace std;
#define ln "\n";

class Array
{
    int *a;
    int n;
    Array();
    Array();
    friend istream& operator >> (istream &x, Array &y);
    friend ostream& operator << (ostream &x, Array y);
};

int main()
{
    return 0;
}
